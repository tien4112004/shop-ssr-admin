export const API_SERVER = 'http://localhost:3000';

const USERS_ENDPOINT = `${API_SERVER}/users`;

const ACCESS_ENDPOINT = `${API_SERVER}/access`;

const PROFILE_ENDPOINT = `${API_SERVER}/profile`;

const REVENUE_ENDPOINT = `${API_SERVER}/revenue`;

export { USERS_ENDPOINT, ACCESS_ENDPOINT, PROFILE_ENDPOINT, REVENUE_ENDPOINT };
